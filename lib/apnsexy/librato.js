// Generated by CoffeeScript 1.4.0
(function() {
  var Librato, Metrics, key, value, _ref,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  _ref = require('./common');
  for (key in _ref) {
    value = _ref[key];
    eval("var " + key + " = value;");
  }

  Metrics = require('librato-metrics');

  module.exports = Librato = (function(_super) {

    __extends(Librato, _super);

    function Librato(options) {
      this.finish = __bind(this.finish, this);

      var _this = this;
      this.metrics = Metrics.createClient(options);
      this.resetVars();
      setInterval(function() {
        var gauges;
        gauges = {};
        if (_this.sent === 0 && _this.drops === 0 && _this.errors === 0) {
          _this.emit('finish');
        }
        gauges.alerts_sent = {
          value: _this.sent
        };
        gauges.errors = {
          value: _this.errors
        };
        gauges.potential_drops = {
          value: _this.drops
        };
        _this.resetVars();
        return _this.gauges(gauges).fail(function(e) {
          throw e;
        }).fin(function() {
          return _this.resetVars();
        });
      }, 10 * 1000);
    }

    Librato.prototype.bind = function(instance) {
      var _this = this;
      instance.on('finish', this.finish);
      instance.on('error', function() {
        return _this.errors++;
      });
      return instance.on('sent', function() {
        return _this.sent++;
      });
    };

    Librato.prototype.counters = function(counters) {
      return this.post({
        counters: counters
      });
    };

    Librato.prototype.finish = function(counts) {
      var _this = this;
      this.drops = counts.potential_drops;
      return this.gauges({
        drop_pct: {
          value: this.drops / counts.total_notifications
        },
        error_pct: {
          value: counts.total_errors / counts.total_notifications
        }
      }).fail(function(e) {
        throw e;
      });
    };

    Librato.prototype.gauges = function(gauges) {
      return this.post({
        gauges: gauges
      });
    };

    Librato.prototype.post = function(data) {
      var _this = this;
      return defer(function(resolve, reject) {
        return _this.metrics.post('/metrics', data, function(err, response) {
          if (err) {
            return reject(err);
          } else {
            return resolve(response);
          }
        });
      });
    };

    Librato.prototype.resetVars = function() {
      this.drops = 0;
      this.errors = 0;
      return this.sent = 0;
    };

    return Librato;

  })(EventEmitter);

}).call(this);
